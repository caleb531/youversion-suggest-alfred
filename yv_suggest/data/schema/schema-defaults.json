{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Defaults",
  "description": "The default values for workflow preferences",
  "type": "object",
  "properties": {
    "language": {
      "description": "The preferred language to use for Bible data",
      "type": "string",
      "pattern": "^[a-z]{2}(_[A-Z]{2})?$"
    },
    "version": {
      "description": "The preferred version (in the current preferred language) to use as the default for Bible references",
      "type": "integer",
      "minimum": 1
    }
  },
  "required": ["language", "version"],
  "additionalProperties": false
}
